<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:contentDescription="@string/widget_content_description">

    <TextView android:id="@+id/widget_empty_text_view"
        android:text="@string/add_games_to_tracking_text"
        android:background="@color/mainBackground"
        style="@style/emptyViewTextStyle"/>

    <StackView android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:loopViews="true" />

</LinearLayout>